# Backup-serveur-safe
Script qui permet de cloner un serveur a l'autre donc une copie d'un serveur d'ou vous n'avez pas les roles et crée le clone sur le votre. |||  script that allows you to clone one server to another, so a copy of a server where you don't have the roles and creates the clone on yours.
